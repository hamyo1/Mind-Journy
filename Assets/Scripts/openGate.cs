using UnityEngine;

public class openGate : MonoBehaviour
{
    public int SumStars = 8;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if (globalVarible.Instance.points == SumStars)
        {

        }
    }
}
